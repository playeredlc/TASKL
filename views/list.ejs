<%- include("header"); -%>

  <div id="heading" class="box">
    <h1> <%= date %> </h1>      
  </div>
  <div class="box">
    <% for(var i=0; i<item.length;i++){ %>
      <div class="item">
        <input type="checkbox">
        <p> <%= item[i] %> </p>
      </div>
      <% } %>
      <form class ="item" action="/" method="POST">
        <input type="text" name="newItem" placeholder="New item" autocomplete="off">
        <button type="submit" name="submitBttn">+</button>
      </form>
  </div>

<%- include("footer"); -%>
